# pymoto

A framework for experimenting with Kuramoto oscillator networks.

Currently an empty scaffold. The oscillator/coupling/integration code
duplicated between `experiments/mnist` and `experiments/cartpole` (see
[`../docs/kuramoto-oscillators.md`](../docs/kuramoto-oscillators.md) for the
underlying theory) will be extracted here as it stabilizes.
